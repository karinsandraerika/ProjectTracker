@page
@model ProjectTracker.Pages.ProjectDetailsModel
@{
}

<h1>@Model.project.Name</h1>



<dl class="row">
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.project.Description)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.project.Description)
    </dd>
</dl>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProjectItems[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectItems[0].Description)
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.ProjectItems[0].Importance)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProjectItems[0].Completed)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProjectItems[0].StartDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProjectItems[0].EndDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProjectItems[0].TimeToComplete)
                </th>*@
                <th>
                    @Html.DisplayNameFor(model => model.ProjectItems[0].Persons)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ProjectItems)
        {
            <tr>
                <td>
                    @*@Html.DisplayFor(modelItem => item.Name)*@
                    <a href="/ProjectItemDetails?id=@item.Id">@Html.DisplayFor(modelItem => item.Name)</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.Importance)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Completed)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StartDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EndDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TimeToComplete)
                    </td>*@
            <td>
                @foreach (var person in item.Persons)
                {
                    @Html.DisplayFor(modelItem => person.Name)
                    if (person != item.Persons.Last())
                    {
                        @Html.Raw(", ")
                    }
                }
            </td>
                <td>
                    @*<a asp-page="./Index" asp-route-id="@item.Id">Project</a> |
                        <a asp-page="./Persons" asp-route-id="@item.Id">Persons</a> |*@
                    <form method="post">
                        <button class="btn btn-danger" asp-page-handler="Delete" asp-route-id="@item.Id" type="submit">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

    @*<form method="post">
            <input type="hidden" asp-for="ProjectId" />
            <button asp-page-handler="AddItem" asp-route-id="@Model.ProjectId" type="submit">Add item</button>
        </form>*@

    <a class="btn btn-success" role="button" asp-page="./CreateProjectItem" asp-route-id="@Model.ProjectId">Add Item</a>

